import React from "react";

function Heading() {
  return <h1>My Favourite Foods</h1>;
}

// Note how we do not use the () as we want to use the output as it is, rather than have to call it
export default Heading;
